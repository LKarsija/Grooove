# Navidrome API Examples

Generated by [`scripts/test-navidrome.ps1`](../../scripts/test-navidrome.ps1).

| File | Description |
|------|-------------|
| `ping.json` | Server handshake / version |
| `albums.json` | `getAlbumList2` response |
| `album.json` | `getAlbum` response (tracks for one album) |
| `stream.txt` | Stream URL for first track (local only, contains auth token) |
| `coverart.txt` | Cover art URL (local only, contains auth token) |

Re-run the smoke test after library changes to refresh the JSON files.

**Note:** `stream.txt` and `coverart.txt` are gitignored because they include signed auth parameters.
