# Navidrome local credentials
scripts/config.local.ps1

# Navidrome runtime data
docker/data/

# Local music library (dev only)
music/

# Generated URLs contain auth tokens (JSON examples are safe to commit)
docs/api_examples/*.txt
