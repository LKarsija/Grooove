# Copy to config.local.ps1 and fill in your Navidrome credentials.
# config.local.ps1 is gitignored and never pushed.

$Server = "http://localhost:4533"
$Username = "admin"
$Password = "YOUR_PASSWORD"
